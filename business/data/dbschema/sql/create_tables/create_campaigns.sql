CREATE TABLE IF NOT EXISTS campaigns (
                                         id INT AUTO_INCREMENT PRIMARY KEY,
                                         name VARCHAR(255),
                                         domain VARCHAR(255)
);

